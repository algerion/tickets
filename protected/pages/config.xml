<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <authorization>
    <allow pages="asunto" users="*" />
    <allow pages="Home, Usuarios.cambiapass" users="@" />
	<allow pages="registro, nota" roles="registro" />
	<allow pages="cobranza, caja" roles="cobranza" />
	<allow pages="entrega, paquetes" roles="entrega" />
	<allow pages="Productos.Agregar, Productos.Modificar" roles="productos" />
    <deny users="*" />
  </authorization>
</configuration>
